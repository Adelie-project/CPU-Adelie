language: cpp

branches:
  only:
    - master

notifications:
  email: false

matrix:
  include:
    - os: linux
      dist: trusty
      compiler: gcc
      addons:
        apt:
          sources: ['ubuntu-toolchain-r-test']
          packages: ['g++-7', 'ocaml-nox']

script:
  - ./scripts/travis.sh

